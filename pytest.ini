[pytest]
testpaths = tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*
asyncio_mode = auto

# Configure test coverage reporting
addopts = 
    --cov=.
    --cov-report=term-missing
    --cov-fail-under=80
    -v 